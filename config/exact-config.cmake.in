#
# exact-config.cmake
#

# "Exact" root directory.
set(EXACT_ROOT_DIR "@CMAKE_INSTALL_PREFIX@")

# Location of include files.
set(EXACT_INCLUDE_DIRS "${EXACT_ROOT_DIR}/include")

# Location of use-exact.cmake file.
set(EXACT_USE_FILE "${EXACT_ROOT_DIR}/share/exact/use-exact.cmake")
